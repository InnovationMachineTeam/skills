# Context and Maintenance Impact

Separate:

- always-loaded name and description;
- on-trigger SKILL.md instructions;
- conditionally loaded references and prompts;
- scripts executed without loading their source;
- tool results, source artifacts, and generated outputs.

Estimate bundle size separately from loaded context per task. Identify dynamic facts, refresh cadence, dependency drift, host variance, eval upkeep, ownership, and retirement conditions. Do not state token savings or latency improvements without comparable measurements.
