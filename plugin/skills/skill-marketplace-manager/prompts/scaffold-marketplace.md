# Route: scaffold-marketplace

Create only the approved local skeleton.

Before writing, confirm repository root, marketplace name, owner identity if supplied, current categories, plugin boundary, and version authority. Do not invent personal email, license, repository URL, or publication target.

Create manifests at their harness-defined roots, generate only non-empty or immediately used category directories, and add validation/build helpers when requested. Validate JSON and all local component paths. Return the created tree and all decisions left as placeholders.
