# Composition Prompt

Keep component skills independently invocable and introduce a bounded router or orchestrator only when a combined user journey adds value.

Define routing, sequencing, shared inputs, handoff contracts, state ownership, recursion limits, partial failure, and completion. Do not copy component instructions into the composer or silently broaden its permissions.
