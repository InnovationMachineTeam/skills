interface:
  display_name: "Prompt Master"
  short_description: "Reconstruct and improve prompts with evidence"
  default_prompt: "Use $prompt-master to reconstruct or improve this prompt and produce an evaluation-backed prompt package."
